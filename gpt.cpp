#include <stdio.h>
#include <stdlib.h>

int uniqueDigits(int num1, int num2) {
    int digits[10] = {0}; // ?????? ??? ???????? ?????????? ????
    int result = 0;

    // ??????? ??? ?????????? ???? ?? ????? ? ??????
    void addDigits(int num){
        while (num > 0) {
            int digit = num % 10; // ???????? ????????? ?????
            digits[digit] = 1; // ???????? ????? ??? ?????????
            num /= 10; // ??????? ????????? ?????
        }
    }

    // ???????? ??????? ??? ????? ?????
    addDigits(num1);
    addDigits(num2);

    // ????????? ????? ?? ?????????? ????
    for (int i = 0; i < 10; i++) {
        if (digits[i] == 1) {
            result = result * 10 + i; // ????????? ????? ? ?????????
        }
    }

    return result;
}

int main() {
    int num1 = 12345;
    int num2 = 67890;
    int result = uniqueDigits(num1, num2);
    printf("?????????: %d\n", result); // ?????: 0123456789

    num1 = 112233;
    num2 = 445566;
    result = uniqueDigits(num1, num2);
    printf("?????????: %d\n", result); // ?????: 123456

    return 0;
}
